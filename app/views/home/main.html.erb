<section class="main-section">
  <%= form_with model: @url do |form| %>
    <%= form.url_field :target, placeholder: 'http://www.example.com', required: true %>

    <%= form.submit 'encurtar url' %>
  <% end %>
</section>
